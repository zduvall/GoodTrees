extends ../layout.pug
include ../utils.pug

block content
  h1.header #{user.username}'s Forest
  .user-info-container
    .sidebar
      h2.sub-header Climber score: 
      .score #{climberScore}
      h2.sub-header Trees in forest: 
      .score #{climbedTrees.length + wantToClimbTrees.length}
    if climbedTrees.length || wantToClimbTrees.length
      .main-content  
        div
          h2.sub-header Climbed trees:
          div: +treeListView(climbedTrees)
        div
          h2.sub-header Trees to climb:
          div: +treeListView(wantToClimbTrees)
    else
      div
        a(href="/trees") Grow your forest by adding trees you have climbed or want to climb!